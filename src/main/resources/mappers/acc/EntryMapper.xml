<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- ============================================
  - 작성자   : 김희정
  - 최초작성 : 2025-06-20
  - 설명     : EntryMapper query xml
  [ 변경 이력 ]
  - 2025-06-20 (김희정): xml 파일 생성
=============================================== -->
<mapper namespace="com.eflix.acc.mapper.EntryMapper">
	<!-- 전표 전체조회 -->
	<select id="getList" resultType="EntryMasterDTO">
    SELECT
      m.entry_number,
      m.entry_type,
      m.entry_date,
      m.entry_status,
      m.creator,
      m.created_at,
      m.updated_at,
      m.transaction_type,
      m.partner_code,
      m.electronic_type,
      m.approval_number,
      m.item_name,
      m.quantity,
      m.unit_price,
      m.total_supply_amount,
      m.total_tax_amount,
      m.remarks,
      d.line_number,
      d.entry_number,
      d.partner_code,
      d.partner_name,
      d.account_code,
      d.resentmen_type,
      d.amount,
      d.description,
      d.created_at,
      d.updated_at
    FROM entry_master m left outer join entry_detail d
    ON m.entry_number = d.entry_number
    ORDER BY m.entry_number;
	</select>
</mapper>